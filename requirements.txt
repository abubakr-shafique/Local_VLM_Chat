# Core ML stack — install torch with CUDA support FIRST (see README.md)
torch>=2.4.0
transformers>=4.51.0
accelerate>=1.5.0
bitsandbytes>=0.45.0
safetensors>=0.4.0
sentencepiece>=0.2.0

# GUI
gradio>=5.20.0

# Images / utilities
Pillow>=10.0.0
huggingface_hub>=0.26.0
