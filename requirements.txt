# Core ML stack — install torch with CUDA support FIRST (see README.md)
# NOTE: Qwen3.5 models require a recent transformers — run: pip install -U transformers
torch>=2.4.0
transformers>=4.51.0
accelerate>=1.5.0
bitsandbytes>=0.45.0
safetensors>=0.4.0
sentencepiece>=0.2.0

# Qwen VL utilities (required for Qwen2.5-VL models)
qwen-vl-utils>=0.0.8

# GUI — Gradio 6.x
gradio>=6.0.0

# Images / utilities
Pillow>=10.0.0
huggingface_hub>=0.26.0